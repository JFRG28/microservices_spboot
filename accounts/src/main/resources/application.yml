server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

build:
  version: "@project.version@"

accounts:
  message: "Welcome to PacoBank-Accounts microservice"
  contactDetails:
    name: "JFRG28"
    email: "fgrodriguez.28@gmail.com"
  onCallSupport:
    - 911
    - 066
