server:
  port: 9000
spring:
  application:
    name: cards
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: qa

build:
  version: "3.0"

cards:
  message: "Welcome to PacoBank-Cards microservice-Default profile"
  contactDetails:
    name: "JFRG28-Developer"
    email: "developer@pacobank.com"
  onCallSupport:
    - 911
    - 066
