spring:
  application:
    name: configserver
  profiles:
    # active: native
    active: git
  cloud:
    config:
      server:
        native:
          # search-locations: classpath:/config/
          # search-locations: file:///C://Users//paco//Downloads//config
        git:
          uri: "https://github.com/JFRG28/test-ms-spboot-config.git"
          default-label: "main"
          timeout: 5
          clone-on-start: true
          force-pull: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"

encrypt:
  key: "DqJ8N5HU6V2mpm"

server:
  port: 8071